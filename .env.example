# Optional backend URL. Leave blank for the frontend-only UI preview.
VITE_API_BASE=

# Optional: enable real Google Sign-In (Google Identity Services).
# Create an OAuth 2.0 Client ID at https://console.cloud.google.com/apis/credentials
# and add your dev origin (e.g. http://localhost:5173) to "Authorized JavaScript origins".
# Leave blank for the frontend-only UI preview.
VITE_GOOGLE_CLIENT_ID=
